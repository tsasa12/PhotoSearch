<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item android:id="@+id/menu_item_searchID"
          android:title="@string/searchString"
          app:actionViewClass="android.support.v7.widget.SearchView"
          app:showAsAction="ifRoom"
        />

    <item android:id="@+id/menu_item_clearID"
          android:title="@string/clear_searchString"
          app:showAsAction="never"
          />

    <item android:id="@+id/menu_item_toggle_pollingID"
          android:title="@string/start_pollingString"
          app:showAsAction="ifRoom"
          />

</menu>